# 署名与来源

## 一、原作者与来源

| 项 | 内容 |
|---|---|
| 原作品 | **墨月写卡器**（分发包标题《送君一程，有缘自会再相见》） |
| 原作者 | **三明月** |
| 本仓库 | 该作品的**移植修改版**，由第三方制作，**不是原作者的官方发布** |

## 二、许可

原项目对许可的声明有两处，均指向同一结论：

1. 原包 `源码/web/package.json`：`"license": "GPL-3.0-only"`；
2. 原包 `使用说明.md` 原文：

> 本项目原创程序代码按 GPL-3.0-only 发布，参见 LICENSE。第三方依赖、Node.js 与酒馆相关项目仍遵循各自许可；第三方图像、名称等不因程序使用 GPL 而改变权利归属。修改并发布程序时，请遵守 GPL 的源码提供与署名要求。

因此：

- 本仓库整体以 **GNU GPL-3.0-only** 发布，许可证全文见 [`LICENSE`](LICENSE)；
- 依据 GPL 第 5 条，本仓库**显著标注自己是一份修改版**，并在下面逐项列出修改内容；
- 完整对应源码即本仓库本身（`墨月_harness\`）；
- 未使用原作者名义进行任何背书或推广。

## 三、改了什么（逐项）

### 1. 原样保留

- `墨月_harness\工具\vendor\script-workshop\` 下 **13 个 `.ts` 源文件**（agent / catalog / export / floating-window / ownership / phone-app-guide / phone-app / phone-shell / phone / project / runtime / storage / worldbook），**未做任何逻辑改动**，只为让 Node 的 ESM 加载器认得它们而补了相对导入的 `.ts` 扩展名（由 `工具\vendor-workshop.mjs` 生成）。
- **全部创作知识文字未改写**：从原包 `源码/web/public/prompt-bundle.json` 抽出为 `墨月_harness\知识库\` 下的 74 个 Markdown 文件，另生成目录 `_索引.md` 与 `_路由表.md`。

### 2. 逐字移植（行为一致性由差分对拍确认）

移植到 `墨月_harness\工具\lib\*.mjs` 的原实现：

- 提示词装配顺序、核心常驻段、来源分离契约、工件维修契约、按域附加契约、上下文带入选派；
- 任务路由（`sectionTaskFilter` 等）与交付闸门（工具裁剪式的权限判定）；
- 成品审阅与四段式处方、YAML 映射检查、MVU 五份文件检查、状态栏／前端 HTML 检查、EJS 检查；
- 打包成卡：`chara` / `ccv3` 数据块内嵌、世界书条目、正则、脚本、固定注入内容；
- 稳定 ID 生成（FNV 双通道 → UUID v4 形状）；
- 会话记忆的注入规则与"摘要没变短就整份丢弃"的安全阀。

一致性验证方式：`墨月_harness\工具\differential\` 下的差分脚本把**原实现与移植版在同一批用例上逐字段对拍**，覆盖审阅 68 例、打包 13 例、结构检查 21 例、路由 391 例、装配 264 例。原实现副本不随仓库分发，用 `setup-*-original.mjs` 指向你自己的墨月源码目录即可重建。

### 3. 从"网页内部函数"改写为独立命令行程序

原版这些能力都长在网页应用内部（浏览器里调用、依赖 UI 状态）。这里改写为可单独运行的程序：

`check.mjs`（校验）、`pack.mjs`（打包与写入）、`prompt.mjs`（提示词装配）、`thread.mjs`（会话存档）、`attach.mjs`（附件）、`workspace.mjs`（工作区查阅）。

### 4. 新增（原版没有）

- `.agents\skills\moyu-card-writer\SKILL.md`：DSH 驾驶协议（把 `流程\` 里的设计浓缩成可执行步骤）；
- `工具\status.mjs`：作品现状与下一步（**替代**原版的页签状态显示）；
- `工具\new-project.mjs`：新建作品工程；
- `工具\character.mjs`：建立人物空壳记录（原版对应"建立人物"按钮）；
- `工具\material.mjs`：开卡档案（`_规划.md` 事实源 / `_进度.md` 进程状态 / 硬闸 / 写作质量粗筛）；
- `知识库\核心\_写作质量自检.md`：写作质量检查清单（手写件，不由知识导入器生成）；
- `工具\lib\prose-audit.mjs`：机械粗筛（破折号、烂词、情绪宣告等表面特征）；
- `工具\release.mjs`：生成发布版（本目录就是这样生成的）；
- `check.mjs` 增加 `--json/--out`、`prompt.mjs` 增加 `--task/--repair/--previous-artifact`：补上原实现里由网页 UI 承担、命令行缺失的**成品维修轮入口**；
- `验收\`：端到端验收脚本与记录。

### 5. 借用的结构设计（来自 Tavern Cards）

**开卡流程与档案层的骨架**借用了 [ai4rpg/tavern-cards](https://github.com/ai4rpg/tavern-cards)（独立项目，有自己的许可）。
**借的是结构设计，不是代码，也不是文字**——本仓库没有复制该项目的任何源代码或知识条目文本；
写作方法论仍全部来自三明月的墨月知识库。逐项如下：

| 借用的设计 | 在本仓库的落地 |
|---|---|
| **双文件分工**：规划＝"应该是什么"，状态＝"做到哪" | `card\<卡名>\_规划.md`（事实源）＋ `_进度.md`（进程） |
| **修改顺序铁律**：先改规划，再改内容 | 写进 SKILL.md 与 `_计划.md` 模板头部 |
| **"先谈定全局再动笔"**：规划定稿必须停下等作者确认 | `material.mjs --plan --confirmed`；未确认时工具**拒绝**推进正文 |
| **对齐模式**：开卡先问"粗略规划"还是"一次确认" | `_规划.md` §一；`--mode rough\|full` |
| **未定项标记**：留白处打标记，创作时遇到必须先追问 | `[待细化]`（我们对语法做了调整：用方括号而非 `#`，以便与标题区分、并可精确计数） |
| **进度判据**："文件存在但未注册 = 未完成" | `--mark` 校验对应文件**存在且非空**，否则拒绝 |
| **位置／模块分组执行**：一组写完统一扫描 | 人物按人、世界书逐条、其余按模块；模块闭合必审 |
| **两道质量防线**：创作前自检 ＋ 独立上下文事后扫描 | `_写作质量自检.md` 同时供主代理自检与审稿子代理使用 |
| **审稿子代理的人格与输出格式** | SKILL.md §五之二的审稿师契约 |
| **收敛判据**：最多若干轮、无新问题即通过 | `_写作质量自检.md` §三（连续两轮无新违规即通过；最多三轮） |
| **审稿只做减法**：不许要求新增事实 | 同节"绝对边界" |

> 该项目的条目类型分类（世界观／地理／时间线／角色／NPC 等）**未采用**：
> 本仓库沿用墨月的 11 个域与人物五栏，因为它对人物生境的划分更细。
> 它的 `tavern-cards-forge` CLI 与其 `创作规划.yaml` 的具名字段结构也未采用。

### 6. 未移植（及其理由）

| 未移植 | 理由 |
|---|---|
| 网页三栏界面与页签状态（`sectionState()`） | 纯 UI 层；DSH 没有对应的界面容器，功能由 `status.mjs` 的文本汇总**替代**（替代，非等价） |
| iframe 预览、MVU／EJS 的 Web Worker | 依赖浏览器运行环境，无法在命令行里成立 |
| SillyTavern 连接器（原包 `源码/connector`） | 独立的可选组件，与写卡主流程无关 |
| 多协议模型接入（原网页内置的模型路由与各家 SDK） | 由运行环境（DSH）负责，重复实现没有意义 |
| 原包 `app/` 与 `runtime/` 目录 | 原版的打包产物与内嵌 Node 运行时 |

### 7. 有意偏离两项（非疏漏）

1. **不做 token 估算与阈值触发的上下文自动压缩。**
   上下文预算是运行环境（DSH）的职责；在它旁边再搭一套更差的压缩器会互相干扰。工作区只落地它给不了的那部分：按 `(域, 专项, 焦点对象)` 分区持久保存作者对话、把它注入请求、并保留原版那条安全阀（摘要为空／超长／没有真的变短 → 整份丢弃、完整历史不变）。

2. **附件不设体积与数量上限、不做 base64 内联、不构造识图请求。**
   读取能力、体积与上下文预算归运行环境。图片以**文件路径**交付，由执行者自行读取（同一张图默认复用已保存的识别记录，避免每次读出不同结论）。仅保留"超过 32KB 的正文只在当轮进入提示词"这一条与存档体积相关的自我约束。

## 四、第三方依赖

`墨月_harness\工具\` 运行时使用以下依赖（版本与墨月写卡器保持一致）：

| 依赖 | 用途 | 许可 |
|---|---|---|
| `acorn` | JavaScript 语法检查 | MIT |
| `vue` | 状态栏／预览的内置运行库（导出时附带完整版） | MIT |
| `yaml` | YAML 解析与检查 | ISC |
| `zod` | MVU 变量结构检查 | MIT |

各依赖的许可原文随 `npm ci` 安装在 `墨月_harness\工具\node_modules\` 内。原包另外整理的完整第三方许可清单在其分发包的 `第三方许可\` 目录中。

## 五、免责

本文档是阅读原项目许可声明与自带说明后的整理，**不构成法律意见**。若你打算商用或大规模分发，请自行复核 GPL-3.0 的完整条款，或咨询专业人士。
