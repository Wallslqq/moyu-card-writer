# 发布信息

这份目录由 `墨月_harness\工具\release.mjs` 生成于 2026-09-15 15:33:16（UTC）。

- 文件数：1834
- 体积：24.5 MB
- node_modules：已包含（开箱即用；已被 .gitignore 排除，不会提交）

## 放进仓库前请确认

- 不要把 `墨月_harness/状态/` 提交上去——那是使用者自己的作品与对话。
- 不要把 `墨月_harness/工具/node_modules/` 提交上去。
- `墨月_harness/工具/differential/*-original/` 是差分对拍用的原实现副本，同样不要提交。

## 这个目录是怎么来的

它是作者在开发副本里用 `墨月_harness\工具\release.mjs` 生成出来的。该生成器属于作者侧工具（依赖 dev 副本里的 `发布/` 源目录），不随本仓库分发。

## 许可

GPL-3.0-only。原作者：三明月。改动清单见 `署名与来源.md`。
