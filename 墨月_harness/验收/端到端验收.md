# 墨月 Harness · 端到端验收

- 作品夹具：`工具/fixtures/project.fixture.json`
- 运行时间：2026-09-15T11:47:07.579Z

## 一、11 个域的路由与装配

| 域 | 焦点 | 意图 | 路由结果 | 预期 | 权限 | 出片 | 装配 system | 结论 |
|---|---|---|---|---|---|---|---|---|
| overview | — | answer | free_creation | free_creation | answer | 否 | 5936 字符 | ✓ |
| carddata | field:description | answer | free_creation | free_creation | answer | 否 | 5920 字符 | ✓ |
| character | characterNature | answer | airp_character_nature | airp_character_nature | answer | 否 | 9449 字符 | ✓ |
| worldbook | wb-1 | artifact | worldview_small | worldview_small | artifact | 是 | 10144 字符 | ✓ |
| rules | — | answer | creation_rules | creation_rules | answer | 否 | 9748 字符 | ✓ |
| opening | — | answer | opening_style_then_draft | opening_style_then_draft | answer | 否 | 6301 字符 | ✓ |
| mvu | crossCheck | answer | mvu_cross_check | mvu_cross_check | answer | 否 | 6074 字符 | ✓ |
| statusbar | — | artifact | mvu_statusbar_native_build | mvu_statusbar_native_build | artifact | 是 | 13546 字符 | ✓ |
| frontend | — | artifact | frontend_build | frontend_build | artifact | 是 | 9401 字符 | ✓ |
| ejs | ejs-1 | artifact | ejs_build | ejs_build | artifact | 是 | 8292 字符 | ✓ |
| package | — | answer | free_creation | free_creation | answer | 否 | 5974 字符 | ✓ |

> 11 个域全部选出唯一专项并完成装配。`overview` / `package` 的专项列表里只有 `free_creation`（等价墨月 `sectionTaskFilter`），
> 它们**照常路由并注入知识与方法，但永不出片**——那是 §二 的 `artifactIntent` 规则在管，不是路由。

## 二、各域已保存成品的确定性校验

| 检查项 | 错误数 | 结论 |
|---|---|---|
| 人物五栏目（文本类外壳） | 0 | ✓ 通过 |
| 世界书条目（正文 + 配对配置） | 0 | ✓ 通过 |
| 创作规则（配置键完整） | 0 | ✓ 通过 |
| 开场白（首条消息） | 0 | ✓ 通过 |
| MVU 变量结构脚本 | 0 | ✓ 通过 |
| MVU 三文件交叉 | 0 | ✓ 通过 |
| 状态栏 HTML | 0 | ✓ 通过 |
| 消息前端（提示词 + HTML 成套） | 0 | ✓ 通过 |
| EJS 工件 | 0 | ✓ 通过 |

## 四、真实生成回路的产物校验

由真实 agent 消费装配出的请求、产出成品，再交给 `check` 判定。清单见 `验收/generated.json`。

| 产物 | 出产方式 | 期望 | 实际 | 结论 |
|---|---|---|---|---|
| 验收/generated.worldbook.md | 子代理（回退读取知识原文） | 通过 | 通过 | ✓ |
| 验收/generated.worldbook.v2.md | 子代理（真实消费 request.worldbook.md） | 拒绝 | 拒绝（2 项：没有找到独立且闭合的正文代码块） | ✓ |

> 这张表是**闸门价值的直接证据**：第二行那条成品内容本身成立，但没按合同用代码块交付，
> 于是被拦下并给出四段式处方——而不是悄悄写进作品。

## 五、与墨月原件的逐格比对（差分测试）

把墨月的**原始 TS 源码**直接加载进 Node（类型擦除），与 harness 移植版喂同一批用例，逐字段比较结果。
不是"照文档写的"，而是**同一份代码的两个副本对跑**。

| 比对对象 | 用例数 | 一致 | 差异 | 结论 |
|---|---|---|---|---|
| compare | 68 | 68 | 0 | ✓ 逐字段一致 |
| compare-pack | 13 | 13 | 0 | ✓ 逐字段一致 |
| compare-validate | 21 | 21 | 0 | ✓ 逐字段一致 |
| compare-route | 391 | 391 | 0 | ✓ 逐字段一致 |
| compare-prompt | 264 | 264 | 0 | ✓ 逐字段一致 |

覆盖范围：审阅（世界书配置 / 规则配置 / 开场白 / YAML / Zod 结构 / 状态栏 / 前端 / EJS / 文本外壳 / 原卡改写）、
MVU 三文件交叉检查、`projectCard()` 的 13 种变体、`validateProject()` 的 21 种作品状态、
路由与意图推断的 391 项，以及**装配层 264 项**（23 个用例 × 11 个结构特征：标签顺序 / 知识正文 / 合同 / 权限 / MVU 文件集 / 批量段 / 维修段 / 自查段 / 代码修复段 / 工作流状态段 / 世界书投递段 / 直达尾指令）。

> 装配层比的是**结构与选择结果**，不比两处有意偏离带来的说明文字差异（运行环境头改为无网页版、工具段改为 harness 工具名）——
> 那些段落只比"在不在"，正文是否逐字相同由各自模块头部记录。

## 六、批处理层 workflow 实跑（2 轮）

编排脚本：`流程/批处理.workflow.js`（逐项生成 → 逐项自校验 → 整组准备待确认稿）。记录见 `验收/batch-runs.json`。

**第 1 轮 · 整组被拒（闸门拦下真实违规）**（3 个 agent，项：钟楼、码头）

- 第 2 项把 3 个世界书条目塞进一个 item；逐项 check 通过（worldview_medium 允许正文多块），但整组准备按契约拒绝：
- 闸门输出：`✗ 整组检查未通过：「码头」必须只含一个正文与配对配置；多条内容分别放入 items。`

**第 2 轮 · 整组准备成功并写入**（3 个 agent，项：钟楼、码头）

- 第 2 项执行者自述：原文件塞了 3 条正是被整组拒绝的原因，本项已按契约收敛为唯一 1 条 —— 闸门拒绝 → 编排纠偏 → 重生成合规的闭环成立。
- 准备输出：`✓ 整组检查通过，已放入待确认区：2026-09-14T18-04-05-804Z-batch:worldbook-b9b18a（2 项）`
- 写入输出：`✓ 已一起写入 2 条世界书内容`
- 写入后世界书：钟楼_地点设定(keyword,order60) / 码头_地区设定(always,order1)
- 重复确认：`✗ 这份内容已经写入过（…）不要重复确认。 exit=1`
- 导出卡条目数：11

> 第 1 轮是**两层校验不可互相替代**的直接证据：逐项 check 通过（`worldview_medium` 允许正文多块），
> 但整组要求"每项恰一个条目"，于是整批被拒——编排据此把整组硬规则写进了每项提示词。

## 七、脚本工坊导出链

由 `验收/verify-workshop.mjs` 独立验证：**15/15 项通过**。

- 含工坊模块的作品能导出**可运行的运行包**（四个框架函数被 `toString()` 序列化内联，约 75 KB，且是可解析的合法 JavaScript）
- 配套世界书条目带 `moyu_script_project` 来源标记、落在指定深度、常驻判定正确
- 手机格式条目存在时注入手机显示正则，id 稳定且与原件一致
- 上一版导出残留的工坊条目被过滤（不重复注入）
- 同作品两次导出逐字段一致

> 实现方式：工坊源码以 **vendor 原件**引入（GPL-3.0-only，与本 harness 同源），不做手抄重写——
> 这段代码会被 `toString()` 序列化进玩家端的卡里，手抄错一个字符离线测试全绿、真机直接跑挂。
> `工具/vendor-workshop.mjs` 可复现地重新生成，只补 Node ESM 需要的 `.ts` 扩展，不改任何逻辑。

## 八、打包链路

由 `工具/fixtures/verify-pack.mjs` 独立验证（见 `验收/` 下的卡产物）：
- 角色卡 JSON 导出与 PNG 内嵌读回**逐字段一致**
- 六类固定注入（initvar 条目 / 变量列表 / 变量输出格式 / MVU 加载器 + 变量结构脚本 / 变量清洗正则 / 状态栏与前端显示正则）逐项落地
- 9 条世界书条目的位置与 order 与墨月一致

## 九、作品结构检查（检查与导出域）

对夹具作品执行 `validateProject()`：1 条结论，其中 0 个阻断、0 个提醒。

- `ready` **[package]** 结构检查通过｜当前正式资料已经满足角色卡打包条件。

> 与成品级校验的分工：成品级看"这一份稿子能不能写入"，结构检查看"整个作品能不能打包"。
> CLI：`node 工具/check.mjs --project <作品.json>`。

## 十、结论

✓ 11 个域全部通过路由、装配与已保存成品校验。

## 边界声明

1. **全部为本地静态检查**，不代表已在 SillyTavern 中运行通过；真实渲染、MVU 实际更新、正则实际替换仍需真机验收。
2. 完成判据写的是「与墨月网页版逐格比对」。本验收做的是**与墨月原始 TS 源码对跑**（五层 757 项，逐字段相等），**未运行墨月的 Web 界面**。
   该口径已由驾驶员裁定认可（2026-09-14）：「在 harness 里复刻墨月的写卡流程、工具与知识就行」。
3. 两处有意偏离 1:1 的理由都写在对应模块头部：① 不做 token 估算与阈值压缩（`工具\lib\threads.mjs`，上下文预算是运行环境的职责）；② 附件不设体积/数量上限、不 base64 内联、不构造识图请求（`工具\lib\attachments.mjs`，读取能力与上下文预算归运行环境）。
   另有一处**有意替代**：原工坊 `agent.ts` 自带 agent 循环与工具调用，而这本就是运行环境的职责，因此由装配器 `prompt.mjs` + 子代理 + `workspace.mjs` 承担，不作为缺口。

复现全部证据：

```
node 验收/run-acceptance.mjs
node 工具/check.mjs --self-test
node 工具/pack.mjs --self-test
node 工具/prompt.mjs --self-test
node 工具/workspace.mjs --self-test
node 工具/thread.mjs --self-test
node 工具/attach.mjs --self-test
node 工具/differential/compare.mjs
node 工具/differential/compare-pack.mjs
node 工具/differential/compare-validate.mjs
node 工具/differential/compare-route.mjs
node 工具/differential/compare-prompt.mjs
node 验收/verify-workshop.mjs
```
