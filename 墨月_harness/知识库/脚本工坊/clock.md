---
id: clock
kind: specialty
group: 小手机
domain: 脚本工坊
name: 时钟与计时
requires: []
---

# 时钟与计时

**依赖**：无

## 业务边界

固定时钟和计时器，不以计时推动剧情。离开应用后不重绘其他应用的输入框，停用清理计时器。 可复用 ctx.phoneApp 的固定基础实现；调用 read_framework 查看当前 APP 的模板和配置。定制时保留作者已确认内容，模板不等于无需验收。

## 必测

- 空内容与长列表
- 保存、取消与重开
- 错误输入和重复操作
- 实际接收与分支切换
